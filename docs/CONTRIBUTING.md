# Contribuindo para OutletIA

## Setup Desenvolvimento

```bash
git clone https://github.com/vinicius898/outletia-complete.git
cd outletia-complete
npm install
cd backend && npm install
cd ../frontend && npm install
